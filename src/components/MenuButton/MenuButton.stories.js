import React from 'react';
import MenuButton from './MenuButton';

export default {
  title: 'Components',
};

export const MenuButtonStory = () => <MenuButton />;

MenuButtonStory.story = {
  name: 'MenuButton',
};
