import React from 'react';
import Footer from './Footer';

export default {
  title: 'Footer',
};

export const footer = () =>
  <Footer title={'course1'}></Footer>;

  Footer.story = {
    name: 'Footer',
  };